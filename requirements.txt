# Core dependencies for text-based invoice processing
pypdf>=3.0.0  # Modern PyPDF2 replacement
pdfplumber>=0.9.0  # Robust PDF extraction with fallback
pandas>=1.5.0
numpy>=1.21.0

# Configuration and data handling
PyYAML>=6.0
python-dateutil>=2.8.0

# User interface (optional)
tkinter  # Usually included with Python

# Flask Web GUI - OneMed Fakturabehandling
Flask>=2.3.0
Werkzeug>=2.3.0

# Streamlit Web GUI - OneMed Fakturabehandling (Anbefalt)
streamlit>=1.28.0
PyPDF2>=3.0.0  # Fallback PDF extraction

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Packaging (for creating EXE)
pyinstaller>=5.0.0

# Note: OCR dependencies (tesseract, easyocr, opencv) have been removed
# since we now use text-based processing instead of image-based OCR
